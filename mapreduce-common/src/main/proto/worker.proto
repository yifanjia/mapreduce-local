syntax = "proto3";

option java_package = "com.yifan.api";

service WorkerService {
  rpc uploadFile (stream FileChunk) returns (UploadStatus);
  rpc heartBeat (stream Empty) returns (HeartbeatResponse);
}

message FileChunk {
  bytes data = 1;
}

message UploadStatus {
  bool success = 1;
  string message = 2;
}

message Empty {}

message HeartbeatResponse {
  string workerId = 1;
  bool isAlive = 2;
  string message = 3;
}